apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svc
  namespace: {{ .Values.http.namespace | default "default" }}
spec:
  ports:
  - port: 80
    targetPort: {{ .Values.http.port | default 3000 }}
  selector:
    app: {{ .Release.Name }}
